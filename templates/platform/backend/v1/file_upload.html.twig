{% extends 'platform/backend/v1/base.html.twig' %}

{% block main %}
    <h1>Upload File</h1>

    {{ form_start(form) }}
    {{ form_row(form.file) }}
    <button class="btn btn-primary">Upload</button>
    {{ form_end(form) }}
{% endblock %}
